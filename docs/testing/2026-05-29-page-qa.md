# Page QA — Manual UI/UX Test Pass

**Date:** 2026-05-29
**Method:** Chrome automation (Mac mini), driven by Claude, reviewed by user.
**Depth:** Deep/interactive — load, console check, layout/dark-mode, primary controls, modals, forms (no destructive submits), edge cases (empty/long content, error paths).
**Viewports:** Desktop ~1440px and Mobile. NOTE: on this Mac mini monitor the Chrome window clamps to a ~500px minimum, so the "mobile" pass was run at **500px** (large-phone / small-tablet). This still triggers the mobile breakpoint (bottom tab bar shows), but true 360–390px phones were not directly captured.
**Fix cadence:** Log all → fix in batch.

Legend: ✅ pass · ⚠️ minor issue · ❌ broken · ⏭️ skipped · — not yet

---

## Coverage matrix

| # | Route | Desktop | Mobile | Notes |
|---|-------|---------|--------|-------|
| 1 | `/login` | — | — | |
| 2 | `/forgot-password` | — | — | |
| 3 | `/reset-password` | — | — | |
| 4 | `/dashboard` | ✅ | ✅ | No console errors. TWG row → workspace nav works. DAYS TO SUMMIT shows "TBD" (P3). Mobile: bottom tab bar shows, KPI strip wraps, no doc overflow. P3: "View all TWGs" truncates to "View al". |
| 5 | `/actions` | ✅ | — | Action tracker (verified earlier in session — KPI strip, filters, table). |
| 6 | `/notifications` | ✅ | — | Master-detail layout, unread badge, detail pane + "Go to Detail" all render. |
| 7 | `/profile` | ✅ | — | Profile + Settings tabs work. P3: Settings model field shows "gpt-4o-mini" but runtime uses gpt-5.5 (cosmetic). Mutations not submitted. |
| 8 | `/deal-pipeline` | ✅ | ⚠️ | Desktop verified earlier. Mobile: table→card conversion works, toolbar/KPI scroll horizontally, no doc overflow. **P2: project-card title row doesn't wrap — long names clip on both sides + AI SCORED badge cut.** |
| 9 | `/deal-pipeline/buyers` | ✅ | — | Verified earlier (buyer table). |
| 10 | `/deal-pipeline/incubation` | ✅ | — | Card grid + KPIs + checklist progress render. Copilot collapses to floating button here (layout variant). |
| 11 | `/deal-pipeline/new` | ✅ | — | Full form: required markers, info tooltips, Leaflet site map (dark-mode OK), upload zone, ESG. Submit not tested (creates project). |
| 12 | `/deal-pipeline/:projectId` | ✅ | — | Verified earlier (project detail: stage tracker, scoring, tabs, Mark flagship/Advance/Generate memo actions). |
| 13 | `/deal-pipeline/:projectId/edit` | ✅ | — | Pre-populated edit form, site map pin, value-chain chips. Submit not tested. |
| 14 | `/deal-pipeline/:projectId/memo` | ❌ | — | **P2: wrong project name "West African Rail Link"** in breadcrumb/subtitle (actual = Office du Niger). **P2: AI memo generates empty** ("No memo generated yet"). |
| 15 | `/schedule` | ✅ | — | Verified earlier (month calendar, meeting chips, conflict legend). |
| 16 | `/meetings/:id` | ✅ | — | Verified earlier (meeting detail: agenda/minutes/participants/docs tabs, send/cancel/edit). |
| 17 | `/meetings/:id/live` | ✅ | — | Live transcript + agenda monitor + Martin command center (OFFLINE state renders fine). |
| 18 | `/twgs` | ✅ | — | Verified earlier (Secretariat Martin multi-agent chat with @mentions). |
| 19 | `/twgs/:id` | ✅ | — | Per-TWG agent chat (Agriculture Martin) + quick actions + context panel. |
| 20 | `/workspace/:id` | ✅ | — | All 5 tabs work (Meetings/Actions/Docs/Members/Subgroups). No console errors. Add-Member submit skipped (creates accounts). |
| 21 | `/assistant` | ⚠️ | — | Renders well, but agent stats/refs look like demo data (1,240 tokens, 98%, fake refs). Verify backend wiring (P2). |
| 22 | `/documents` | ✅ | ✅ | Desktop verified earlier. Mobile: `.doc-table` column-hiding works (Document + actions only), filters/search fit, no overflow. |
| 23 | `/knowledge-base` | ⚠️ | — | **Mock data** — "Cobalt project", "Mining Regulation TWG", Oct-2023 dates don't match real WAIIS data. Appears unwired (P2). |
| 24 | `/conflicts` | ⚠️ | — | "COMING SOON" overlay, BUT underlying `ProjectConflicts.tsx:40` fires repeated "Failed to fetch conflicts" AxiosErrors (broken/unimplemented endpoint, retry loop). P2. |
| 25 | `/admin/control-tower` | ✅ | — | Real wired data (56 mtgs/60 docs/41 projects), weekly packet, conflict detection. Solid. |
| 26 | `/admin/team` (Team) | ✅ | — | Roster + KPIs + role dropdowns. P3: many leftover Test Admin/Normal seed users. |
| 27 | `/admin/invitations` (Org Invitations) | ✅ | — | Clean empty state, KPIs, filters. |
| 28 | `/admin/logs` (Audit Logs) | ✅ | — | Real audit trail, filters, date grouping. P3: IP Address column always empty. |
| 29 | `/invitations/:id/respond` | — | — | |
| 30 | `404` (unknown route) | ✅ | — | No dedicated 404 page — catch-all redirects to /dashboard (acceptable). |

---

## Issues log

> One entry per finding. Severity: P1 (broken/blocker) · P2 (clear UX issue) · P3 (polish/nice-to-have).

| ID | Route | Viewport | Sev | Issue | Status |
|----|-------|----------|-----|-------|--------|
| I1 | `/deal-pipeline/:id/memo` | desktop | **P2** | Breadcrumb + subtitle show placeholder "West African Rail Link" instead of the actual project name (Office du Niger). Page isn't binding the real project name. | open |
| I2 | `/deal-pipeline/:id/memo` | desktop | **P2** | AI memo generation returns empty → "No memo generated yet" after the spinner. `generate_investment_memo` yields 0 chars (Azure gpt-5.5 empty completion). | open |
| I3 | `/conflicts` | desktop | **P2** | "COMING SOON" overlay, but `ProjectConflicts.tsx:40` fires repeated "Failed to fetch conflicts" AxiosErrors in a retry loop (unimplemented endpoint + no cleanup). | open |
| I4 | `/knowledge-base` | desktop | **P2** | Shows mock/demo data ("Cobalt project", "Mining Regulation TWG", Oct-2023 dates) that doesn't match real WAIIS data. Appears unwired to backend. | open |
| I5 | `/assistant` | desktop | **P2** | Agent stats + references look like static demo data (1,240 tokens, 98% accuracy, fake refs). Verify backend wiring. | open |
| I6 | `/deal-pipeline` | mobile | **P2** | Project-card title row doesn't wrap — long project names clip on both sides and the "AI SCORED" badge is cut. | open |
| I7 | `/dashboard` | both | P3 | "DAYS TO SUMMIT" shows "TBD" (summit date passed / unset). | open |
| I8 | `/dashboard` | mobile | P3 | "View all TWGs" link truncates to "View al". | open |
| I9 | `/profile` (Settings) | desktop | P3 | Model-name field shows "gpt-4o-mini" but runtime uses Azure gpt-5.5 (cosmetic mismatch). | open |
| I10 | `/admin/team` | desktop | P3 | Roster polluted with many leftover "Test Admin"/"Test Normal" seed users (data hygiene). | open |
| I11 | `/admin/logs` | desktop | P3 | IP Address column always empty (IPs not captured/displayed). | open |

---

## Notes / observations

-
